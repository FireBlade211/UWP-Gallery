<UserControl
    x:Class="UWPGallery.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPGallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    xmlns:models="using:UWPGallery.DataModel"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <converters:CollectionVisibilityConverter
            x:Name="collectionConverter"
            EmptyValue="Collapsed"
            NotEmptyValue="Visible" />

        <converters:EmptyObjectToObjectConverter
            x:Name="objectBoolConverter">
            <converters:EmptyObjectToObjectConverter.EmptyValue>
                <x:Boolean>False</x:Boolean>
            </converters:EmptyObjectToObjectConverter.EmptyValue>

            <converters:EmptyObjectToObjectConverter.NotEmptyValue>
                <x:Boolean>True</x:Boolean>
            </converters:EmptyObjectToObjectConverter.NotEmptyValue>
        </converters:EmptyObjectToObjectConverter>

        <converters:EmptyObjectToObjectConverter
            x:Name="objectVisibilityConverter">
            <converters:EmptyObjectToObjectConverter.EmptyValue>
                <Visibility>Collapsed</Visibility>
            </converters:EmptyObjectToObjectConverter.EmptyValue>

            <converters:EmptyObjectToObjectConverter.NotEmptyValue>
                <Visibility>Visible</Visibility>
            </converters:EmptyObjectToObjectConverter.NotEmptyValue>
        </converters:EmptyObjectToObjectConverter>

        <converters:StringFormatConverter x:Key="stringFormatConverter" />
    </UserControl.Resources>
    <Grid x:Name="headerGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="4">
            <TextBlock
                AutomationProperties.AutomationId="PageHeader"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="{x:Bind Item.Title, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"
                TextWrapping="NoWrap"
                AutomationProperties.HeadingLevel="Level1" />
            <Button
                x:Name="APIDetailsBtn"
                Margin="0,0,0,3"
                Padding="4"
                VerticalAlignment="Bottom"
                AutomationProperties.Name="API details"
                ToolTipService.ToolTip="API details"
                Background="Transparent">
                <FontIcon
                    FontSize="14"
                    Foreground="{ThemeResource SystemAccentColor}"
                    Glyph="&#xE946;"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                <Button.Flyout>
                    <Flyout Placement="Bottom">
                        <Flyout.FlyoutPresenterStyle>
                            <Style TargetType="FlyoutPresenter">
                                <Setter Property="MinWidth" Value="420" />
                                <Setter Property="MaxWidth" Value="800" />
                            </Style>
                        </Flyout.FlyoutPresenterStyle>
                        
                        <StackPanel x:Name="APIPanel" Spacing="16">
                            <StackPanel
                            x:Name="APISourcePanel"
                            Spacing="8"
                            Visibility="{x:Bind Item.ApiNamespace, Converter={StaticResource emptyStringToVisibilityConverter}, Mode=OneWay}">
                                <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BaseTextBlockStyle}"
                                Text="Namespace" />
                                <TextBlock
                                VerticalAlignment="Center"
                                FontFamily="Consolas"
                                IsTextSelectionEnabled="True"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind Item.ApiNamespace, Mode=OneWay}" />
                            </StackPanel>
                            <MenuFlyoutSeparator Margin="-12" Visibility="{x:Bind Item.BaseClasses, Converter={StaticResource collectionConverter}, Mode=OneWay}" />
                            <StackPanel x:Name="InheritanceSourcePanel" Visibility="{x:Bind Item.BaseClasses, Converter={StaticResource collectionConverter}, Mode=OneWay}"
                                        Spacing="8">
                                <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BaseTextBlockStyle}"
                                Text="Inheritance" />

                                <TextBlock FontFamily="Consolas" TextTrimming="CharacterEllipsis" Style="{StaticResource CaptionTextBlockStyle}"
                                        IsTextSelectionEnabled="True" Text="{x:Bind GetInheritanceString(), Mode=OneWay}"/>
                            </StackPanel>
                            <MenuFlyoutSeparator Margin="-12"
                                                 Visibility="{x:Bind Item.Requirements, Converter={StaticResource objectVisibilityConverter}, Mode=OneWay}" />
                            <StackPanel x:Name="RequirementsSourcePanel"
                                        Visibility="{x:Bind Item.Requirements, Converter={StaticResource objectVisibilityConverter}, Mode=OneWay}"
                                        Spacing="8">
                                <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource BaseTextBlockStyle}"
                                Text="Requirements" />

                                <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Minimum OS Build"
                                Visibility="{x:Bind Item.Requirements.OSBuild, Converter={StaticResource objectVisibilityConverter}, Mode=OneWay}"/>
                                
                                <TextBlock
                                VerticalAlignment="Center"
                                Visibility="{x:Bind Item.Requirements.OSBuild, Converter={StaticResource objectVisibilityConverter}, Mode=OneWay}"
                                Text="{x:Bind Item.Requirements.OSBuild, Converter={StaticResource stringFormatConverter}, ConverterParameter='{}{0}', Mode=OneWay}"
                                FontFamily="Consolas" TextTrimming="CharacterEllipsis"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                IsTextSelectionEnabled="True"/>

                                <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Required API Contracts"
                                Visibility="{x:Bind Item.Requirements.ApiContracts, Converter={StaticResource collectionConverter}, Mode=OneWay}"/>

                                <TextBlock FontFamily="Consolas" TextTrimming="CharacterEllipsis" Style="{StaticResource CaptionTextBlockStyle}"
                                        IsTextSelectionEnabled="True" Text="{x:Bind GetApiContractString(), Mode=OneWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,12,0,12">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <DropDownButton
                    AutomationProperties.Name="Documentation"
                    ToolTipService.ToolTip="Documentation"
                    Visibility="{x:Bind Item.Docs, Converter={StaticResource collectionConverter}, Mode=OneWay}">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="16" Glyph="&#xE7C3;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                            <TextBlock Text="Documentation" />
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <Flyout Placement="Bottom">
                            <ItemsControl
                                x:Name="DocsList"
                                Margin="6"
                                IsTabStop="False"
                                ItemsSource="{x:Bind Item.Docs, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:ControlInfoDocLink">
                                        <HyperlinkButton
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            DataContext="{x:Bind Uri, Mode=OneWay}"
                                            Click="HyperlinkButton_Click"
                                            ToolTipService.ToolTip="{x:Bind Uri, Mode=OneWay}">
                                            <TextBlock Text="{x:Bind Title, Mode=OneWay}" />
                                        </HyperlinkButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Flyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
                <DropDownButton AutomationProperties.Name="Source code" ToolTipService.ToolTip="Source code of this sample page">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Viewbox Height="18">
                                <PathIcon Data="{StaticResource GitHubIconPath}" />
                            </Viewbox>
                            <TextBlock Text="Source" />
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <Flyout Placement="Bottom">
                            <StackPanel x:Name="SourcePanel" Margin="0,-8,0,-12" Padding="0,0,0,12">
                                <StackPanel
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal"
                                    Spacing="8">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="Sample page source code" />
                                    <Button
                                        Padding="6,5,6,6"
                                        ToolTipService.ToolTip="Source code of this sample page in the UWP Gallery repository"
                                        AutomationProperties.Name="Info"
                                        Background="Transparent">
                                        <FontIcon
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            Foreground="{ThemeResource SystemAccentColor}"
                                            Glyph="&#xE946;" />
                                    </Button>

                                </StackPanel>
                                <HyperlinkButton
                                    x:Name="PageMarkupGitHubLink"
                                    Margin="6,4,-12,0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    ToolTipService.ToolTip="{x:Bind PageMarkupGitHubLink.Tag, Mode=OneWay}">
                                    <TextBlock Text="XAML" />
                                </HyperlinkButton>
                                <HyperlinkButton
                                    x:Name="PageCodeGitHubLink"
                                    Margin="6,4,-12,0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    ToolTipService.ToolTip="{x:Bind PageCodeGitHubLink.Tag, Mode=OneWay}">
                                    <TextBlock Text="C#" />
                                </HyperlinkButton>
                            </StackPanel>
                        </Flyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    x:Name="ShareButton"
                    Height="32"
                    Margin="0,0,4,0"
                    Padding="11,2,11,0"
                    AutomationProperties.Name="Share"
                    AutomationProperties.FullDescription="Share this sample"
                    ToolTipService.ToolTip="Share this sample"
                    Click="ShareButton_Click">

                    <FontIcon Glyph="&#xE72D;" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="16" />
                </Button>

                <AppBarSeparator/>

                <Button
                    x:Name="CopyLinkButton"
                    Height="32"
                    Margin="0,0,4,0"
                    Padding="11,2,11,0"
                    AutomationProperties.Name="Copy link"
                    Click="OnCopyLinkButtonClick"
                    ToolTipService.ToolTip="Copy link">

                    <FontIcon Glyph="&#xE71B;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Button>
                <Button
                    x:Name="FeedbackBtn"
                    Height="32"
                    Margin="0,0,4,0"
                    AutomationProperties.Name="Send feedback"
                    Click="OnFeedBackButtonClick"
                    ToolTipService.ToolTip="Send feedback">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xED15;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </Button.Content>
                </Button>
                <AppBarSeparator />
                <ToggleButton x:Name="FavoriteButton"
                              Height="32"
                              Margin="4,0,0,0"
                              Click="FavoriteButton_Click"
                              AutomationProperties.Name="Favorite sample">
                    <ToggleButton.Content>
                        <FontIcon FontSize="16"
                                  FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="{x:Bind GetFavoriteGlyph(FavoriteButton.IsChecked), Mode=OneWay}" />
                    </ToggleButton.Content>
                    <ToolTipService.ToolTip>
                        <ToolTip Content="{x:Bind GetFavoriteToolTip(FavoriteButton.IsChecked), Mode=OneWay}" />
                    </ToolTipService.ToolTip>
                </ToggleButton>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
