<Page
    x:Class="UWPGallery.SearchResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPGallery"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Pivot Title="SEARCH RESULTS FOR NULL" x:Name="MainPivot">
        <PivotItem x:Name="AllItem" Header="All (0)">
            <Grid>
                <GridView
                x:Name="AllGV"
                Margin="12,24,0,0"
                Padding="0,0,0,36"
                AutomationProperties.AutomationId="ResultsGridView"
                AutomationProperties.Name="Search Results"
                IsItemClickEnabled="True"
                IsSwipeEnabled="False"
                ItemTemplate="{StaticResource ControlItemTemplate}"
                SelectionMode="None"
                TabIndex="1"
                ItemClick="AllGV_ItemClick"/>

                <TextBlock Text="No results match your search." HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" x:Name="NoResultsTextBlock"/>
            </Grid>

        </PivotItem>
    </Pivot>
</Page>
