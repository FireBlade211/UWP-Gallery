<Page
    x:Class="UWPGallery.ControlPages.AppBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPGallery.ControlPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:controls="using:UWPGallery.Controls">

    <Page.Resources>
        <local:NullableObjectToBoolConverter x:Key="nullableObjectToBoolConverter"/>
    </Page.Resources>

    <Grid RowSpacing="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock TextWrapping="WrapWholeWords" Text="To use an AppBar, create an AppBar control and assign it to the Page.TopAppBar or Page.BottomAppBar property."/>
        <TextBlock TextWrapping="WrapWholeWords" Grid.Row="1" Text="Once an AppBar is added to a page, you can right-click anywhere within the page or swipe up from the bottom to show it."/>

        <ListBox x:Name="SampleListBox" Grid.Row="2"/>
    </Grid>

    <Page.BottomAppBar>
        <AppBar>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <AppBarButton x:Name="DeleteButton" IsEnabled="{x:Bind SampleListBox.SelectedItem, Converter={StaticResource nullableObjectToBoolConverter}, Mode=OneWay}"
                                  Click="DeleteButton_Click"/>
                    <AppBarButton x:Name="AddButton" Label="Add" Icon="Add" Click="AddButton_Click"/>
                </StackPanel>

                <AppBarButton x:Name="RefreshButton" Grid.Column="1" HorizontalAlignment="Right" Icon="Refresh" Label="Refresh" Click="RefreshButton_Click"/>
            </Grid>
        </AppBar>
    </Page.BottomAppBar>
</Page>
